import { describe, it, expect, vi } from 'vitest'

describe('Sabrina\'s Playlist Quality Tests', () => {
    it('should be at least 8 hours long', () => {
        expect(1 + 2).toBe(3);
    });
    it('should have at least 100 Brazil pop funk songs', () => {
        expect(1 + 2).toBe(3);
    });
    it('should have at least 3 hours of Brazil rock n roll music', () => {
        expect(1 + 2).toBe(3);
    });
});